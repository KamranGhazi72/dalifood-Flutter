import 'package:flutter/material.dart';

class FoodDescBody extends StatelessWidget {
  const FoodDescBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
